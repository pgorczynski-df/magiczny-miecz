import { BoxObject } from "../BoxObject";

export class CardStack extends BoxObject {

  constructor(topTexture: string, width: number, aspect: number, height: number) {
    super(topTexture, width, aspect, height);
  }

}
